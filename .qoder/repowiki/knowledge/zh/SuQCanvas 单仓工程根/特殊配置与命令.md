单仓统一入口：`npm run dev` 启动在线版，`npm run dev:lan` 启动局域网版（自动复制 pdf.js 资源并监听 0.0.0.0）；`npm run build:online` / `build:lan` / `build:all` 分别产出 `dist/` 与 `dist-lan/`；`npm run package:lan` 生成含 Node 运行时的 release ZIP；`npm run lan` 单独启动局域网中继；`npm run lan:open` 打开 Windows 防火墙入站规则。