- 通过约定式全局对象 `window.__pipCtrl` 作为 PiP 窗口与宿主播放器之间的桥接接口，避免直接耦合具体播放器实现。
- PiP 窗口内的交互统一采用事件委托 + `[data-a]` data 属性分发到对应动作（toggle/next/prev/close/seek）。
- 对跨窗口访问的 DOM 元素进行存在性检查后再操作，并在 `onunload` 中清理定时器与引用以防止内存泄漏。