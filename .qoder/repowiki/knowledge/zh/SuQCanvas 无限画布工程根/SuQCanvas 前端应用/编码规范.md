- 跨模块共享状态统一放在 store/ 下，通过 Zustand selector 订阅，避免组件间直接传参
- 外部依赖（LAN/OSS/Supabase）集中在 sync/ 中封装，上层组件仅通过 store 或 hook 间接调用
- 媒体相关能力（音频分析、封面色、播放列表、Blob 注册）统一归入 media/ 并由 store 与 canvas 节点复用
- 可复用的业务逻辑配套单元测试（clipboard、importExport、fuzzySearch、playlists、coverColor、fileKind 等），测试文件与源文件同名并列