需先安装 ffmpeg（Windows 推荐 winget install Gyan.FFmpeg）。一键录制：`node promo/record/run.mjs [--fps 12] [--out <path>] [--skip-build]`；分步流程见 `record/README.md`。动画演示录制：`npm run record`（即 `node capture.mjs`）。演示项目生成：`node promo/apple/build-demo.mjs`，素材缺失时先运行 `node promo/apple/fetch-photos.mjs`。