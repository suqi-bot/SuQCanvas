# 2026-09-02 工作日志

## SuQCanvas v1.3.0 — B 分组/容器 + C 对齐参考线 开发与独立 QA 完成

**背景**：用户询问「下一步该做什么」，经产品路线图评估后，确认推进 B（分组/容器）走标准 SOP + C（对齐参考线）走快速模式，并行开发。

**关键事实（持久价值）**：
- 技术栈：React 19 + TS + Vite 8 + Zustand + Dexie + @xyflow/react v12 + Tailwind v4。在线版 Supabase+OSS，局域网版 ws 中继。
- B（分组/容器）完全复用 React Flow v12 原生 group node（`parentId`+`extent:'parent'`+相对坐标），**零新增依赖**，**.sqcanvas / Dexie 零 schema 变更**（分组/命名/锁定仅为 `SuqNodeData` 可选字段）。
- B 核心文件：`src/canvas/groups.ts`（13 纯函数：computeAbsolutePosition/reparentToGroup/dissolveGroup/clampChildrenToParent/normalizeGroupHierarchy 等）、`src/canvas/nodes/GroupNode.tsx`、`src/components/{GroupToolbar,LockToggle,GroupInspectorSection}.tsx`、`src/store/canvasStore.ts`（groupSelected/dissolveGroup/deleteGroupWithDescendants/setNodeLocked）、`src/sync/lanClient.ts`（锁定广播）、`src/io/importExport.ts`（导入归一化）。设计文档：`docs/system_design.md`。
- C（对齐参考线）核心：`src/canvas/alignment/{alignGuides.ts, alignmentGuideStore.ts, AlignmentGuides.tsx}` + `settingsStore.showAlignmentGuides` 开关 + `CanvasBoard.onNodeDrag` 接入。

**QA 闭环（重要）**：B 第 1 轮独立 QA（严过关）发现 1 个源码 Bug——嵌套成组错位（`reparentToGroup` 内 `byId` 只含被选中节点、漏算祖先，导致相对坐标被当绝对）。第 2 轮修复（给 `reparentToGroup` 加第 4 参数 `allNodes`，`groupSelected` 传完整 nodes）后回归通过。全量测试 99 → 100 通过。教训：**成组/坐标换算必须传入完整 nodes 以计算祖先链**。

**状态**：B+C 均 IS_PASS=YES，代码均在工作区，未 git 提交。下一步可选：git 提交（两笔 feat）、补 P2（Ctrl+G 快捷键/折叠展开）、锁定远端 draggable 实时同步。

**团队（software-company 专家 SOP）**：product-manager 许清楚（PRD）/ architect 高见远（架构）/ engineer 寇豆码（实现+修复）/ qa 严过关（双功能独立验证）。
